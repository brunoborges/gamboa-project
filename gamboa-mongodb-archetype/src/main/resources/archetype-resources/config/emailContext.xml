<?xml version="1.0" encoding="UTF-8"?>
<beans default-autowire="byName"
	xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<context:property-placeholder location="classpath:email.properties" ignore-unresolvable="true" />

	<bean id="noReplySender" class="org.springframework.mail.javamail.JavaMailSenderImpl"
		lazy-init="true">
		<!-- change these values if you don't have a Google GMail account -->
		<property name="host" value="${mail.smtp.host}" />
		<property name="port" value="${mail.smtp.port}" />

		<!-- username/password -->
		<property name="username" value="${mail.smtp.username}" />
		<property name="password" value="${mail.smtp.password}" />

		<!-- You don't need to change this, unless your SMTP is not SSL -->
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.auth">${mail.smtp.auth}</prop>
				<prop key="mail.smtp.starttls.enable">${mail.smtp.starttls.enable}</prop>
			</props>
		</property>
	</bean>

	<bean id="signUpEmailSettings" class="code.services.email.EmailSettings">
		<property name="sender" ref="noReplySender" />
		<property name="from" value="${mail.from}" />
		<property name="person" value="${artifactId}" />
		<property name="subject" value="Sign up completed" />
		<property name="template" value="templates/signup-success.vm" />
	</bean>

	<bean id="contactEmailSettings" class="code.services.email.EmailSettings">
		<property name="sender" ref="noReplySender" />
		<property name="from" value="${mail.from.noreply}" />
		<property name="to" value="${mail.to.contact}" />
		<property name="person" value="${artifactId}" />
		<property name="subject" value="Web site contact" />
		<property name="template" value="templates/contact.vm" />
	</bean>

</beans>

